{
  "name": "explainly",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered Chrome extension to explain anything on hover",
  "scripts": {
    "check": "node scripts/validate-env.js",
    "build:extension": "node scripts/build-extension.js",
    "release:alpha": "node scripts/release.js alpha",
    "release:beta": "node scripts/release.js beta",
    "release:prod": "node scripts/release.js prod"
  },
  "engines": {
    "node": ">=20"
  }
}
